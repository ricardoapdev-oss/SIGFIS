# Copie este arquivo para .env e ajuste os valores antes de subir o sistema.
# Comando: cp .env.example .env

# Segredo JWT — troque por uma string longa e aleatória em produção
JWT_SECRET=sigecontratos_jwt_troque_em_producao_2026

# Acesso ao pgAdmin (interface visual do banco)
PGADMIN_EMAIL=admin@iquego.com.br
PGADMIN_PASSWORD=admin_iquego_2026
