version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: sigecontratos_postgres
    restart: always
    environment:
      POSTGRES_USER: sigecontratos_user
      POSTGRES_PASSWORD: sigecontratos_password
      POSTGRES_DB: sigecontratos_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    container_name: sigecontratos_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@sigecontratos.com
      PGADMIN_DEFAULT_PASSWORD: admin_password
    ports:
      - "5050:80"
    depends_on:
      - postgres

volumes:
  postgres_data:
